metastore_entity.metastore_item_add:
  rout_name: entity.metastore_item.add_form
  title: 'Add metadata'
    # Where will the link appear, defined by route name.
  appears_on:
    - entity.metastore_item.collection