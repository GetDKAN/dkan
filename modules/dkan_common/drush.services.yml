services:
  common.commands:
    class: \Drupal\dkan_common\Commands\CommonCommands
    tags:
      - { name: drush.command }
    arguments:
      - '@dkan.common.dataset_info'

  common.jobstore.commands:
    class: \Drupal\dkan_common\Commands\JobStoreCommands
    tags:
      - { name: drush.command }
    arguments:
      - '@database'
