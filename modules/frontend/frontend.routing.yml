frontend.dataset:
  path: '/dataset/{name}'
  defaults:
    _controller: 'Drupal\frontend\Controller\Page::dataset'
  requirements:
    _access: 'TRUE'

route_callbacks:
  - 'frontend.route_provider::routes'
