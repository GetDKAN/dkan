imports:
  -  behat.yml

default:
  extensions:
    Behat\MinkExtension:
      base_url: http://web
      selenium2:
        wd_host: http://browser:4444/wd/hub
    Drupal\DrupalExtension:
      drupal:
        drupal_root: %paths.base%/../../docroot
