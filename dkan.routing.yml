dkan.3.metastore.schemas:
  path: '/api/3/metastore/schemas'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::getSchemas'}
  requirements:
    _permission: 'access content'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id:
  path: '/api/3/metastore/schemas/{identifier}'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::getSchema'}
  requirements:
    _permission: 'access content'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items:
  path: '/api/3/metastore/schemas/{schema_id}/items'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::getAll'}
  requirements:
    _permission: 'access content'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.post:
  path: '/api/3/metastore/schemas/{schema_id}/items'
  methods: [POST]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::post'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::get'}
  requirements:
    _permission: 'access content'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.publish:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}/publish'
  methods: [PUT]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::publish'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.put:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}'
  methods: [PUT]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::put'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.patch:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}'
  methods: [PATCH]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::patch'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.delete:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}'
  methods: [DELETE]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::delete'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.revisions:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}/revisions'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreRevisionController::getAll'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.revisions.id:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}/revisions/{revision_id}'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreRevisionController::get'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.3.metastore.schemas.id.items.id.revisions.post:
  path: '/api/3/metastore/schemas/{schema_id}/items/{identifier}/revisions'
  methods: [POST]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreRevisionController::post'}
  requirements:
    _permission: 'post put delete datasets through the api'
  options:
    _auth: ['basic_auth', 'cookie']


dkan.3.metastore.schemas.dataset.items.id.docs:
  path: '/api/3/metastore/schemas/dataset/items/{identifier}/docs'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::getDocs'}
  requirements:
    _permission: 'access content'
  options:
    _auth: ['basic_auth', 'cookie']

metastore.data_json:
  path: '/data.json'
  methods: [GET]
  defaults:
    { _controller: '\Drupal\dkan\Controller\MetastoreController::getCatalog'}
  requirements:
    _permission: 'access content'
  options:
    _auth: ['basic_auth', 'cookie']

dkan.metastore.config_properties:
  path: '/admin/dkan/properties'
  defaults:
    _form: '\Drupal\dkan\Form\DkanDataSettingsForm'
    _title: 'Metastore referencer configuration'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE

metastore.data_dictionary:
  path: '/admin/dkan/data-dictionary'
  defaults:
    _controller: '\Drupal\system\Controller\SystemController::systemAdminMenuBlockPage'
    _title: 'Data-Dictionary'
  requirements:
    _permission: 'create data content'

metastore.data_dictionary.settings:
  path: '/admin/dkan/data-dictionary/settings'
  defaults:
    _title: 'Data-Dictionary Settings'
    _form: '\Drupal\dkan\Form\DataDictionarySettingsForm'
  requirements:
    _permission: 'administer site configuration'